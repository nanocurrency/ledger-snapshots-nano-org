<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>Nano Ledger Snapshots</title>
    <link>https://ledger-snapshots.nano.org/</link>
    <description>Torrent releases of Nano ledger snapshots. Subscribe in a torrent client with built-in RSS auto-download to automatically fetch and seed each new snapshot.</description>
    <language>en-us</language>
    <!-- Rewritten in full by snapshot.py's publish_listing() (snap-gni.18)
         each snapshot cycle, generated from data.json, not hand-edited.
         One item per entry in data.json, newest first. Uses the
         podcast-style enclosure convention (type=application/x-bittorrent)
         rather than relying solely on a magnet link in the description, for
         the broadest torrent-client RSS-downloader compatibility. -->
  </channel>
</rss>
